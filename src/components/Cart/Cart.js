import classes from './Cart.module.css';

const Cart = (props) => {
  const cartItems = [];

  return (
    <div>
      {cartItems}
      <div></div>
      <div></div>
    </div>
  );
};

export default Cart;
